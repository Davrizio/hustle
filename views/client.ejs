<%- include('partials/header') -%>
<div class="container">
  <div class="row justify-content-center mt-5">
    <div class="col-6">
      <h2><%= clients.name %></h2>
      <img class="img-fluid" src="<%= clients.image%>" />
      <div class="row justify-content-between">
        <form
          class="col-1"
          action="/client/likePost/<%= clients.id %>?_method=PUT"
          method="POST"
        >
          <button class="btn btn-primary fa fa-heart" type="submit"></button>
        </form>
        <h3 class="col-3">Likes: <%= clients.likes %></h3>
        <%if(clients.user == user.id){ %>
        <form
          action="/client/deletePost/<%= clients.id %>?_method=DELETE"
          method="POST"
          class="col-3"
        >
          <button class="btn btn-primary fa fa-trash" type="submit"></button>
        </form>
        <%}%>
      </div>
    </div>
    <div class="col-3 mt-5">
      <p><%= clients.address %></p>
    </div>
  </div>
</div>

<%- include('partials/footer') -%>
